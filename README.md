A Python program that uses the built-in Tkinter library for the GUI, to take an incomplete sudoku from the user and solve it. This program makes use of the backtracking algorithm to solve the sudoku.
